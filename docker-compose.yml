version: '3'

services:
  postgresql:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: alvaroico
      POSTGRES_USER: alvaroico
      POSTGRES_DB: db_alvaroico
